#include <gtest/gtest.h>

// Example test - replace with actual tests for your assignment
TEST(TemplateTest, BasicTest) {
    EXPECT_EQ(2 + 2, 4);
    EXPECT_TRUE(true);
}

TEST(TemplateTest, StringTest) {
    std::string hello = "Hello";
    EXPECT_EQ(hello.length(), 5);
}

// Add more tests here for your specific assignment requirements